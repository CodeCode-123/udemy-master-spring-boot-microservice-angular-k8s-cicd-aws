server:
  port: 9094

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: order
  data:
    mongodb:
      uri: mongodb://localhost:27017/orderdb
      host: localhost
      port: 27071
      database: orderdb
      repositories:
        enabled: true

app:
  service:
    url: "http://userservice/user/fetchUserById/"

