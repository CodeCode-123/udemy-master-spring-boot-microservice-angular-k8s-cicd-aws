server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # location /api/ {
    #     # Point to EKS CoreDNS
    #     resolver 10.100.0.10 valid=10s; # Or your cluster's kube-dns IP
        
    #     # Use a variable to force dynamic DNS re-resolution
    #     set $backend_alb http://amazonaws.com;
        
    #     proxy_pass $backend_alb;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    # }

    # location /api/ {
    #     proxy_pass k8s-default-awsingre-9aa4c55379-1554860328.us-east-1.elb.amazonaws.com;
    #     # proxy_pass http://cluster.local; # Route /api/ to your backend service internal DNS (http://cluster.local)
    #     # proxy_http_version 1.1;
    #     # proxy_set_header Host $host;
    #     # proxy_set_header X-Real-IP $remote_addr;
    #     # proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    # }
}

# server {
#     listen 80;
#     server_name localhost;
#     root /usr/share/nginx/html; # Path to your built Angular files
#     index index.html index.htm;

#     location / {
#         try_files $uri $uri/ /index.html;
#     }
# }