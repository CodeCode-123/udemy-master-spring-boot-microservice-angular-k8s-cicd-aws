apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: kafka
  labels:
    app: kafka
#namespace: default
spec:
  serviceName: kafka-headless
  replicas: 3
  selector:
    matchLabels:
      app: kafka
  template:
    metadata:
      labels:
        app: kafka
    spec:
      containers:
        - name: kafka
          image: bitnamilegacy/kafka:3.3.2  # Use your target version
          ports:
            - containerPort: 9092
              name: client
            - containerPort: 9093
              name: controller
          env:
            # 1. Define the node roles
            - name: KAFKA_CFG_PROCESS_ROLES
              value: "broker,controller"

            # 2. Node identity matching the StatefulSet ordinal
            - name: KAFKA_CFG_NODE_ID
              value: "1"

            # 3. FIX: Ensure CONTROLLER is declared here alongside PLAINTEXT
            - name: KAFKA_CFG_LISTENERS
              value: "PLAINTEXT://:9092,CONTROLLER://:9093"

            # 4. Map protocols to security mechanisms
            - name: KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP
              value: "CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT"

            # 5. Tell Kafka which listener name belongs to the controller
            - name: KAFKA_CFG_CONTROLLER_LISTENER_NAMES
              value: "CONTROLLER"

            # 6. Set controller quorum voters
            - name: KAFKA_CFG_CONTROLLER_QUORUM_VOTERS
              value: "1@localhost:9093"

            # 7. Configure advertised listeners for clients (do not put CONTROLLER here)
            - name: KAFKA_CFG_ADVERTISED_LISTENERS
              value: "PLAINTEXT://kafka-0.kafka-headless.default.svc.cluster.local:9092"

            - name: ALLOW_PLAINTEXT_LISTENER
              value: "yes"
---
apiVersion: v1
kind: Service
metadata:
  name: kafka-headless
spec:
  clusterIP: None
  selector:
    app: kafka
  ports:
    - port: 9092
      name: client
    - port: 9093
      name: controller



#apiVersion: apps/v1
#kind: StatefulSet
#metadata:
#  name: kafka
#spec:
#  serviceName: kafka-headless
#  replicas: 3
#  selector:
#    matchLabels:
#      app: kafka
#  template:
#    metadata:
#      labels:
#        app: kafka
#    spec:
#      containers:
#        - name: kafka
#          image: apache/kafka:4.0.0
#          # Override the entrypoint command to parse the trailing digit from hostname
#          command:
#            - /bin/sh
#            - -c
#            - |
#              # Extract the pod index (e.g., "kafka-0" becomes "0")
#              export KAFKA_NODE_ID=${HOSTNAME##*-}
#              # For older ZooKeeper-based setups, also set: export KAFKA_BROKER_ID=${HOSTNAME##*-}
#
#              # Execute the default entrypoint script
#              exec /etc/kafka/docker/run
#          ports:
#            - containerPort: 9092
#              name: kafka
#          env:
#            - name: KAFKA_PROCESS_ROLES
#              value: "broker,controller"
#            - name: KAFKA_NODE_ID
#              valueFrom:
#                fieldRef:
#                  fieldPath: metadata.name
#            - name: KAFKA_CONTROLLER_QUORUM_VOTERS
#              value: "0@kafka-0.kafka-headless:9093,1@kafka-1.kafka-headless:9093,2@kafka-2.kafka-headless:9093"
#            - name: KAFKA_BOOTSTRAP_SERVERS
#              value: "kafka-service:9092"
#            - name: CLUSTER_ID
#              value: "5L6g3nShT-eMCtK--X86sw"
#          resources:
#            requests:
#              memory: "4Gi"
#              cpu: "2"
#            limits:
#              memory: "4Gi"
#              cpu: "2"
#          volumeMounts:
#            - name: data
#              mountPath: /var/lib/kafka/data
#  volumeClaimTemplates:
#    - metadata:
#        name: data
#      spec:
#        accessModes: [ "ReadWriteOnce" ]
#        resources:
#          requests:
#            storage: 100Gi
#---
#apiVersion: v1
#kind: Service
#metadata:
#  name: kafka-service
#spec:
#  clusterIP: None  # <-- This makes it a Headless Service
#  selector:
#    app: kafka
#  ports:
#    - name: broker
#      port: 9092