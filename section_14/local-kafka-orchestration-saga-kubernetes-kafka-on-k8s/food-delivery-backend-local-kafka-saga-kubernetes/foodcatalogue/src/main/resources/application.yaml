server:
  port: 9082

spring:
  profiles:
    active: local
  application:
    name: foodcatalogue-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092,localhost:9094}

    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JacksonJsonSerializer
      acks: all
      properties:
        delivery:
          timeout:
            ms: 120000
        linger:
          ms: 0
        request:
          timeout:
            ms: 30000
        enable:
          idempotence: true
        max:
          in:
            flight:
              requests:
                per:
                  connection: 5
#      transaction-id-prefix: foodcatalogue-service-${random.value}-

    consumer:
      group-id: foodcatalogue-ms
      auto-offset-reset: earliest
      key-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer #org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer #org.springframework.kafka.support.serializer.JacksonJsonDeserializer
      properties:
        spring.json.type.mapping: "com.codecode.restaurantlisting.dto.RestaurantDTO:com.codecode.foodcatalogue.dto.RestaurantDTO,com.codecode.order.dto.command.ReserveFoodCommand:com.codecode.foodcatalogue.dto.command.ReserveFoodCommand"
        spring:
          json:
            trusted:
              packages: "com.codecode.foodcatalogue.dto.*,com.codecode.restaurantlisting.dto.RestaurantDTO,com.codecode.*"
          deserializer:
            key:
              delegate:
                class: org.apache.kafka.common.serialization.StringDeserializer
            value:
              delegate:
                class: org.springframework.kafka.support.serializer.JacksonJsonDeserializer

app:
  kafka:
    food-command-topic: "food-command"
    food-event-topic: "food-event"

logging:
  level:
    org.springframework.kafka.transaction.KafkaTransactionManager: DEBUG
    org.springframework.transaction: DEBUG
    org.springframework.orm.jpa.JpaTransactionManager: DEBUG
    org.apache.kafka.clients.producer.internals.TransactionManager: DEBUG

